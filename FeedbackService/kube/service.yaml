# ... Deployment YAML definition
apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedback-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: feedback-app
      tier: backend
  template:
    metadata:
      labels:
        app: feedback-app
        tier: backend
    spec:
      containers:
        - name: feedback-app
          image: peshmerge/feedback-service:1.0.0
          ports:
            - containerPort: 9091
              name: service-port
          env:
            - name: FEEDBACK_SERVICE_DB
              value: feedback-service-db:3306/feedback_db?autoReconnect=true
            - name: QUESTIONNAIRE_SERVICE_HOST
              value: questionnaier-service:80/v1/questionnaires/
            - name: FEEDBACK_SERVICE_DB_USER
              valueFrom:
                secretKeyRef:
                  name: feedback-servicedb-login
                  key: username
            - name: FEEDBACK_SERVICE_DB_PASS
              valueFrom:
                secretKeyRef:
                  name: feedback-servicedb-login
                  key: password
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              port: service-port
              path: /actuator/health
            initialDelaySeconds: 20
            timeoutSeconds: 5
---
# ... Service YAML definition
apiVersion: v1
kind: Service
metadata:
  name: feedback-service
spec:
  selector:
    app: feedback-app
    tier: backend
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 9091
  type: LoadBalancer